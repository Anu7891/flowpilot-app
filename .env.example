# Copy this file to .env and adjust if needed.
# Local docker DB (matches docker-compose.yml):
DATABASE_URL="postgresql://flowpilot:flowpilot@localhost:5432/flowpilot"
# Session signing secret - any long random string (generate: npx -y nanoid or openssl rand -hex 32)
AUTH_SECRET="change-me-to-a-long-random-string"
